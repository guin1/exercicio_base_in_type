import { FormContainer, HeroTitle } from './styles'

const Hero = () => (
  <FormContainer>
    <div className="container">
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </div>
  </FormContainer>
)

export default Hero
